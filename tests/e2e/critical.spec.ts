import { test, expect } from '@playwright/test';
test('ranking público muestra solo nombres',async({page})=>{await page.goto('/clasificacion');await expect(page.getByText('Clasificación pública')).toBeVisible();await expect(page.getByText('Carlos')).toBeVisible();await expect(page.getByText('@')).toHaveCount(0);});
test('registro y login renderizan',async({page})=>{await page.goto('/registro');await expect(page.getByPlaceholder('Nombre')).toBeVisible();await page.goto('/login');await expect(page.getByText('Continuar con Google')).toBeVisible();});
test('quiniela permite ver pronósticos demo',async({page})=>{await page.goto('/quiniela');await expect(page.getByText('Mis pronósticos')).toBeVisible();await expect(page.getByText('Guardar todos los abiertos')).toBeVisible();});
test('admin modo manual renderiza',async({page})=>{await page.goto('/admin');await expect(page.getByText('Panel administrativo')).toBeVisible();await expect(page.getByText('Guardar resultado').first()).toBeVisible();});
test('perfil completar y recuperar renderizan',async({page})=>{await page.goto('/completar-perfil');await expect(page.getByPlaceholder('Apellido')).toBeVisible();await page.goto('/recuperar-contrasena');await expect(page.getByText('Enviar enlace')).toBeVisible();});
