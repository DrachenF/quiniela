import { redirect } from 'next/navigation';
import { getMatches, getProfile, getUserPredictions } from '@/lib/data';
import { MatchCard } from '@/components/match-card';
export default async function Quiniela(){const profile=await getProfile();if(!profile)redirect('/login');const [matches,predictions]=await Promise.all([getMatches(),getUserPredictions(profile.id)]);const byMatch=new Map(predictions.map((p:any)=>[p.match_id,p]));return <div><h1 className="text-3xl font-black">Mis pronósticos</h1><p className="mt-2 text-slate-600">Horarios en America/Guatemala. Los partidos cierran cinco minutos antes.</p>{!profile.is_active||!profile.profile_completed?<p className="card my-5 p-4 text-amber-700">Tu perfil está incompleto o inactivo. Completa tu perfil para pronosticar.</p>:null}{matches.length===0?<section className="card mt-6 p-6">Todavía no hay partidos disponibles</section>:<div className="mt-5 grid gap-4 md:grid-cols-2">{matches.map(m=><MatchCard key={m.id} match={m} prediction={byMatch.get(m.id)}/>)}</div>}</div>}
