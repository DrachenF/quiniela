import { matches } from '@/lib/mock-data';
export default function Admin(){return <div className="space-y-6"><section className="card p-6"><h1 className="text-3xl font-black">Panel administrativo</h1><p className="text-amber-700">Si FOOTBALL_API_KEY está vacío, usa el modo manual.</p></section><section className="card p-6"><h2 className="text-2xl font-black">Partidos</h2>{matches.slice(0,4).map(m=><div className="mt-3 grid gap-2 border-t pt-3 md:grid-cols-6" key={m.id}><span>{m.round}</span><input className="input" defaultValue={m.homeTeam?.name||'Por definir'}/><input className="input" defaultValue={m.awayTeam?.name||'Por definir'}/><input className="input" placeholder="GL"/><input className="input" placeholder="GV"/><button className="btn">Guardar resultado</button></div>)}</section></div>}
