import { getLeaderboard } from '@/lib/data';
export default async function Clasificacion(){const leaderboard=await getLeaderboard();return <section className="card p-6"><h1 className="text-3xl font-black">Clasificación pública</h1><p className="text-slate-600">Solo se muestra el nombre; nunca apellido, correo ni UUID.</p>{leaderboard.length===0?<p className="mt-6">Todavía no hay participantes con puntos</p>:<div className="mt-6 overflow-x-auto"><table className="w-full text-left"><thead><tr><th>Pos.</th><th>Nombre</th><th>Puntos</th><th>Exactos</th><th>1X2</th><th>Clasif.</th><th>Cont.</th></tr></thead><tbody>{leaderboard.map(r=><tr className="border-t" key={`${r.position}-${r.participantName}`}><td>{r.position}</td><td>{r.participantName}</td><td><b>{r.totalPoints}</b></td><td>{r.exactScores}</td><td>{r.outcomes}</td><td>{r.qualified}</td><td>{r.counted}</td></tr>)}</tbody></table></div>}</section>}
