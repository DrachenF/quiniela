import type { Match, Outcome } from './types';
export function outcome(home:number, away:number): Outcome { return home>away?'HOME':home<away?'AWAY':'DRAW'; }
export function calculateLockAt(kickoffAt:string|Date): Date { return new Date(new Date(kickoffAt).getTime()-5*60_000); }
export function isPredictionLocked(match: Pick<Match,'lockAt'|'kickoffAt'|'status'|'manuallyLocked'>, now=new Date()): boolean { return now.getTime()>=new Date(match.lockAt).getTime() || now.getTime()>=new Date(match.kickoffAt).getTime() || !!match.manuallyLocked || ['LOCKED','LIVE','FINISHED','CANCELLED'].includes(match.status); }
export function scorePrediction(args:{predHome:number;predAway:number;predQualifiedTeamId?:string|null;officialHome?:number|null;officialAway?:number|null;qualifiedTeamId?:string|null;round:string;status:string}){ if(args.status!=='FINISHED'||args.officialHome==null||args.officialAway==null) return {resultPoints:0,qualifiedPoints:0,totalPoints:0,exact:false,outcome:false,qualified:false}; const exact=args.predHome===args.officialHome&&args.predAway===args.officialAway; const outcomeOk=outcome(args.predHome,args.predAway)===outcome(args.officialHome,args.officialAway); const resultPoints=exact?3:outcomeOk?1:0; const qualified=args.round!=='THIRD_PLACE'&&!!args.qualifiedTeamId&&args.predQualifiedTeamId===args.qualifiedTeamId; const qualifiedPoints=qualified?1:0; return {resultPoints,qualifiedPoints,totalPoints:resultPoints+qualifiedPoints,exact,outcome:!exact&&outcomeOk,qualified}; }
export function validatePredictionInput(home:number, away:number, round:string, homeTeamId?:string|null, awayTeamId?:string|null, qualifiedTeamId?:string|null){ if(!homeTeamId||!awayTeamId) throw new Error('No se puede pronosticar equipos Por definir.'); if(!Number.isInteger(home)||!Number.isInteger(away)||home<0||away<0||home>20||away>20) throw new Error('Los goles deben estar entre 0 y 20.'); const predOutcome=outcome(home,away); if(round!=='THIRD_PLACE'&&predOutcome==='DRAW'&&!qualifiedTeamId) throw new Error('Seleccione quién clasifica.'); return {predictedOutcome:predOutcome,predictedQualifiedTeamId: round==='THIRD_PLACE'?null: predOutcome==='HOME'?homeTeamId:predOutcome==='AWAY'?awayTeamId:qualifiedTeamId}; }
